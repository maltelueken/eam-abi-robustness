# @package _global_
hydra:
  run:
    dir: outputs/experiment-1/${hydra.job.override_dirname}
  sweep:
    dir: multirun/experiment-1
    subdir: ${hydra.job.override_dirname}
  job:
    config:
      override_dirname:
        exclude_keys:
          - experiment
