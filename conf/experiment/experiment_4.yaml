# @package _global_

defaults:
  - experiment_1
  - experiment_2
  - experiment_3

diag_num_obs: 100
test_num_obs: 100

# real_data_filename: ${hydra:runtime.cwd}/outputs/${hydra:runtime.choices.experiment}/${hydra:runtime.choices.model}/test_data/FF1.txt
